# Offline work queue — opened 2026-08-05, refreshed 2026-08-06

Written so "what is next" never depends on re-deciding in the moment. Each item
is runnable with no reviewer and no `NVEncCore/` change. Strike items as they
land; add to the bottom rather than reordering.

## Landed

- [x] luma vs chroma temporal decoupling — `FINDINGS-2026-08-06-ONE-DEFECT.md`
- [x] densify truth/synth CV on Shining V, Interstellar V, Scarface V to 16
      frame pairs — retracted the quality-labelled negative, 2/16 mean −0.0113
- [x] does luma over-signal track luma decoupling? — subsumed by the one-defect
      consolidation: log-log corr −0.802, t=−5.54, n=19, slope −0.414
- [x] chroma per-luma-band behaviour — `FINDINGS-2026-08-06-PERBIN.md`
- [x] QVBR generalisation of the `0.01` response margin — 1.03×/1.15×, not the
      3–5× first reported; `FINDINGS-2026-08-05-QVBR-MARGIN.md`
- [x] playback gate — `FINDINGS-2026-08-06-PLAYBACK-VERDICT.md`, no visible
      difference between arms at the most divergent window

## Queued

1. **Locate the floor.** *(in flight — `floor_separation.py`)* The one defect
   has two candidate causes in `NVEncFilterFilmGrain.cu`, both on master since
   `2e164d0e` and neither CLI-reachable: the selection floor at `:2391` and the
   denoise clamp at `:2449`, both at sigma 2.0 on 10-bit. Confirm the
   over-denoise→inflated-curve mechanism by sweeping `denoise` upward, since
   `NVEncCmd.cpp:1181` forbids going below it.
2. **Separate the two floors properly.** Needs a build with `minNoiseLevel`
   lowered — the only way to probe below sigma 2.0. Plan first; this is an
   `NVEncCore/` change even if test-only.
3. **Is the decoupling scene-driven?** Map emitted table intervals onto frames
   (ticks are 1e-7 s; a 288-frame 23.976 clip spans 120,120,000) and test
   whether over-delivery concentrates where the model holds an interval across
   a real change in source grain. Shining V has two intervals at mean 51--53
   against a 8--22 baseline, so the data to check is already on disk.
4. **Densify the two remaining HEVC cells** (Taxi 15000k, Interstellar 15000k)
   to 16 pairs for symmetry with the 6000k cells that were densified.
5. **Chroma texture at 16 pairs.** `FINDINGS-2026-08-05-CHROMA-DIAGNOSIS.md`
   quotes lag-1 `0.020` / lag-2 `0.039` from six pairs. Pooled axes are stable
   at six, but confirming on the three planes that matter costs little.

## Blocked, not queued

- Chroma strength fix itself — touches `NVEncCore/`, out of this session's
  offline-only scope.
- Motion separator promotion — perceptual gate is now **provisionally passed**;
  the remaining blocker is that the candidate is reachable only through
  test-only environment hooks.

## Standing caveats on anything above

Three claims were retracted this session after densification or frame-set
correction (the quality-labelled negative, the 3–5× QVBR degradation, the
spatial-inflation explanation for chroma over-signal). Findings at n≤12, with
collinear variables, or on a single frame set should be re-tested before
anything is built on them.

## Rule

Do not end a turn without an item from this queue running in the background.
An idle turn is a stopped session; the only thing that resumes work is a
background job completing.
