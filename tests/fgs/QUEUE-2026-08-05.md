# Offline work queue — 2026-08-05

Written so "what is next" never depends on re-deciding in the moment. Each item
is runnable with no reviewer and no `NVEncCore/` change. Strike items as they
land; add to the bottom rather than reordering.

## In flight

- [x] luma vs chroma temporal decoupling — does the plane that already has the
      temporal source/base estimate track its source better?
- [x] densify truth/synth CV on Shining V, Interstellar V, Scarface V to 16
      frame pairs; the six-frame limit recorded in
      `FINDINGS-2026-08-05-HEVC-ARTIFACT.md` applies to these CVs too

## Queued

1. **Does luma over-signal track luma decoupling?** Same regression as the
   chroma one, using luma played totals already in
   `FINDINGS-2026-08-04-TEMPORAL-SOURCE-OBSERVATIONS.md`. If luma is both less
   decoupled and better closed, that is independent support for the mechanism.
2. **Is the decoupling scene-driven?** Map emitted table intervals onto frames
   (ticks are 1e-7 s; a 288-frame 23.976 clip spans 120,120,000) and test
   whether over-delivery concentrates where the model holds an interval across
   a real change in source grain. Shining V has two intervals at mean 51--53
   against a 8--22 baseline, so the data to check is already on disk.
3. **Chroma per-luma-band behaviour.** The analyser bins strength by *luma*;
   chroma bands have never been decomposed. A band-level error would show
   whether the decoupling is temporal only or also across intensity.
4. **Densify the two remaining HEVC cells** (Taxi 15000k, Interstellar 15000k)
   to 16 pairs for symmetry with the 6000k cells that were densified.
5. **QVBR generalisation of the `0.01` response margin.** Named as an open
   blocker in `FINDINGS-2026-08-04-TEXTURE-TEMPORAL-STABILITY.md`, calibrated
   only at QVBR 29, and chosen after observing the Shining failure so it is
   post-hoc. Sweep QVBR 25/29/34/39 on the four unseen film scenes.
6. **Chroma texture at 16 pairs.** `FINDINGS-2026-08-05-CHROMA-DIAGNOSIS.md`
   quotes lag-1 `0.020` / lag-2 `0.039` from six pairs. Pooled axes are stable
   at six, but confirming on the three planes that matter costs little.

## Blocked, not queued

- Chroma strength fix itself — touches `NVEncCore/`, out of this session's
  offline-only scope.
- Motion separator promotion — needs the 46 MB playback gate in
  `/media/merged-storage/media/test-encodes/minimal-review-20260805/`.

## Rule

Do not end a turn without an item from this queue running in the background.
An idle turn is a stopped session; the only thing that resumes work is a
background job completing.
